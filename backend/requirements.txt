# Core - keep pinned for reproducibility, but allow minor upgrades
APScheduler==3.10.4
reportlab==4.1.0
fastapi==0.110.2
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.8.2
pydantic-settings==2.4.0
pandas==2.2.2
numpy==1.26.4
# polars is optional for >1M rows (L7), keep but can be skipped in CI if needed
polars==1.10.0
duckdb==1.0.0
plotly==5.22.0
# LLM: openai is base, groq uses same client; others use httpx fallback so no hard dep
openai>=1.54.0
python-dotenv==1.0.1
openpyxl==3.1.5
XlsxWriter==3.2.0
httpx==0.27.2
pytest==8.3.2
pytest-asyncio==0.23.8
anyio==4.4.0
# Optional LLM providers - install only if you need native SDK (httpx fallback works without them)
# groq==0.11.0
# anthropic==0.40.0
# google-generativeai==0.8.2
# L7 Enterprise (optional, graceful fallback if missing)
redis==5.0.1
celery==5.3.6
# L8 Cloud (optional, mocked in tests)
stripe==15.4.0
cryptography==42.0.5
fsspec==2024.6.1
s3fs==2024.6.1
# L09 Data Foundation (Postgres + OTEL) — optional, graceful fallback if missing
sqlalchemy==2.0.30
alembic==1.13.2
psycopg[binary]==3.1.18
asyncpg==0.29.0
aiosqlite==0.20.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
sentry-sdk==2.14.0
moto[s3]==4.2.14
boto3==1.34.130
