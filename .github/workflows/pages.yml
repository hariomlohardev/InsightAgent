name: Pages — showcase + docs
on:
  push:
    branches: [main, master]
    paths: [index.html, docs/**, README.md, ARCHITECTURE.md, BENCHMARKS.md]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          # auto-enable Pages if repo never had it turned on (fixes Not Found)
          enablement: true
      - name: Build showcase (already static)
        run: |
          ls -lh index.html docs/index.html
          test -f .nojekyll && echo ".nojekyll ok"
          test -f docs/.nojekyll && echo "docs/.nojekyll ok"
          # optional Docusaurus build (soft fail if deps missing)
          if [ -f docs/package.json ]; then
            cd docs && npm ci --quiet 2>&1 | head -n 20 || echo "npm ci soft fail"
            npm run build 2>&1 | head -n 50 || echo "docusaurus build soft fail (static index.html still served)"
            ls -lh build 2>&1 | head -n 20 || true
          fi
          # ensure Pages artifact is the showcase (multi-page + redirects)
          mkdir -p /tmp/pages
          cp index.html /tmp/pages/index.html
          # root redirects so /benchmarks.html works as well as /docs/benchmarks.html
          for f in 404.html benchmarks.html blazing.html start.html architecture.html api.html enterprise.html contributing.html; do test -f "$f" && cp "$f" /tmp/pages/"$f" 2>&1 | head -n 5 || true; done
          cp -r docs /tmp/pages/docs 2>&1 | head -n 20 || true
          cp .nojekyll /tmp/pages/.nojekyll 2>&1 | head -n 5 || true
          ls -lh /tmp/pages/ 2>&1 | head -n 30
          ls -lh /tmp/pages/docs/ 2>&1 | head -n 30
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: /tmp/pages
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
