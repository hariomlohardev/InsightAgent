# OpenAI (optional - if not set, uses rule-based fallback - still works)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
STORAGE_PATH=./storage
MAX_UPLOAD_MB=100

# Frontend
BACKEND_URL=http://localhost:8000
